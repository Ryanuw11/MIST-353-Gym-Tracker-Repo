@page
@model Gym_Tracker.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course[0].CourseName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course[0].LengthMinutes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course[0].TrainerFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course[0].TrainerLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course[0].ClassPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Course) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CourseName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LengthMinutes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrainerFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrainerLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClassPrice)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Cid">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Cid">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Cid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
